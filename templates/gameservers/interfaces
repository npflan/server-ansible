# Generated by Ansible : {{ template_run_date }}

source /etc/network/interfaces.d/*

# Loopback device:
auto lo
iface lo inet loopback

# device: eth0
auto eth0
no-auto-down eth0
iface eth0 inet dhcp


auto eth1
allow-hotplug eth1
iface eth1 inet loopback


{% set i = 0 %}
{% for interface in interfaces %}
auto eth1:{{ i|string }}
allow-hotplug eth1:{{ i|string }}
iface eth1:{{ i|string }} inet static
  address {{ interface }}/32

{% set i = i + 1 %}
{% endfor %}
