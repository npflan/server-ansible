# Generated by Ansible : {{ template_run_date }}
# Loopback device:
auto lo
iface lo inet loopback
iface lo inet6 loopback

# device: eth0
auto eth0
iface eth0 inet dhcp

{% set i = 0 %}
{% for interface in interfaces %}
iface eth0:{{ i|string }} inet static
  address {{ interface }}/32
{% set i = i + 1 %}
{% endfor %}